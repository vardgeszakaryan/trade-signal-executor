[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "trade-signal-executor"
version = "0.1.0"
description = "An autonomous system built for trade execution automation."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cryptg>=0.6.0",
    "litellm>=1.94.1",
    "loguru>=0.7.3",
    "metatrader5>=5.0.6090",
    "pydantic>=2.13.4",
    "python-dotenv>=1.0.0",
    "pyyaml>=6.0.3",
    "telethon>=1.44.0",
    "typer>=0.15",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
tse = "cli.main:app"
test-models = "cli.model_eval:main"
